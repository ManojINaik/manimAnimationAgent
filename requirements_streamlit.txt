# Streamlit and FastAPI dependencies
streamlit>=1.28.0
fastapi>=0.104.0
uvicorn>=0.24.0
python-multipart>=0.0.6

# Essential dependencies for Streamlit interface
python-dotenv>=0.19.0
requests>=2.25.0
numpy>=1.21.0
pandas>=1.3.0
pydantic>=2.0.0

# AI/ML dependencies
openai>=1.0.0
litellm>=1.0.0
tqdm>=4.62.0

# Google Gemini support
google-generativeai>=0.8.0
google-cloud-aiplatform>=1.38.0

# Lightweight processing dependencies
pillow>=8.3.0

# Text-to-speech (optional)
elevenlabs>=1.0.0

# Audio processing (lightweight)
soundfile>=0.12.0

# Additional utilities
tiktoken>=0.4.0
langchain>=0.1.0
langchain-core>=0.1.0
langchain-community>=0.0.20
langchain-text-splitters>=0.0.1
langfuse>=2.0.0
chromadb>=0.4.0

# Note: Heavy dependencies like manim, moviepy, pydub are excluded
# to prevent build failures on Streamlit Cloud. The app will gracefully
# handle missing dependencies and show appropriate messages.

# For local development with full video generation:
# Uncomment the following lines and install system dependencies
# manim>=0.17.0
# moviepy>=1.0.3
# pydub>=0.25.1 