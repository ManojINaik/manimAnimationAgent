# GitHub Actions Requirements - Complete dependency list with audio support
# Optimized for video rendering with ALL required dependencies including audio dubbing

# Core Python Libraries
python-dotenv>=1.0.1

# AI/ML/LLM Dependencies
google-generativeai>=0.4.0
openai>=1.12.0
litellm>=1.28.0
anthropic>=0.18.0
tiktoken>=0.4.0

# Video/Animation/Graphics Libraries
manim>=0.18.0
manim-physics
manim-chemistry
manim-ml
manim-dsa
manim-circuit
opencv-python>=4.9.0.80
Pillow>=10.2.0
imageio>=2.34.0
imageio-ffmpeg>=0.4.9
moviepy>=2.0.0
numpy>=1.26.0
scipy>=1.12.0
matplotlib>=3.8.0

# Text/Subtitle Processing
pysrt>=1.1.2
pylatexenc>=2.10

# Audio Processing & TTS (CI/CD compatible)
SpeechRecognition>=3.14.0
pydub>=0.25.1
soundfile>=0.12.1
gTTS>=2.5.0
pyttsx3>=2.90
librosa>=0.10.1

# Audio Libraries (without PyAudio - use alternatives)
# NOTE: For audio dubbing in GitHub Actions, we use:
# - gTTS for basic TTS (no API key needed)
# - pyttsx3 for offline TTS
# - pydub for audio manipulation
# - librosa for audio analysis

# RAG/Vector Database/LangChain
langchain>=0.3.0
langchain-community>=0.3.0
langchain-core>=0.3.0
langchain-text-splitters>=0.3.0
chromadb>=0.6.0

# Database/Storage
appwrite>=4.0.0
requests>=2.31.0

# Web Framework/API
gradio>=4.15.0
fastapi>=0.109.0
uvicorn>=0.27.0

# Data Processing/Utilities
pandas>=2.2.0
tqdm>=4.62.0
python-multipart>=0.0.7
aiofiles>=23.2.1

# Memory and Learning
mem0ai>=0.1.0

# Additional Analysis/Monitoring
langfuse>=2.0.0

# Web search for error-driven development
tavily-python>=0.3.0

# Audio dubbing strategy for GitHub Actions:
# 1. Use gTTS (Google Text-to-Speech) - free, no hardware needed
# 2. Use pyttsx3 as fallback - offline TTS
# 3. Avoid PyAudio/hardware-dependent libraries
# 4. Use pydub for audio file manipulation
# 5. ElevenLabs only if API key is available (graceful fallback)

# Exclude problematic dependencies for CI/CD:
# - PyAudio (requires system audio libraries)
# - TTS libraries that need audio hardware
# - Real-time audio processing libraries
# - ElevenLabs (requires API keys + audio hardware)

manim-voiceover>=0.3.7 